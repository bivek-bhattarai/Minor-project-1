import 'package:flutter/material.dart';

// Constant file for AddNewUser class

// Submit button text
const kSubmitButtonText = Text(
  'Submit',
  style: TextStyle(color: Colors.amber),
);

// Cancel button text
const kCancelButtonText = Text(
  'Cancel',
  style: TextStyle(color: Colors.amber),
);

// TextFormField content padding
const kTextFormFieldPadding = EdgeInsets.all(15.0);

// TextFormField style for enter text
const kTextFieldStyle = TextStyle(fontSize: 25.0);

// Border type for TextFormField
const kTextFieldBorderDecoration = OutlineInputBorder();

// Border style for TextFormField
const kTextFieldBorder =  BorderSide(color: Colors.amber, width: 2.0);

// Label for TextFormField
const kTextFieldLabel = Text('Name');

// Label style
var kTextFieldLabelStyle = const TextStyle(color: Colors.amber);
