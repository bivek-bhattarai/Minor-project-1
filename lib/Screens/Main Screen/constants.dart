import 'package:flutter/material.dart';
import 'package:flutter_spinkit/flutter_spinkit.dart';

const kListTileContentPadding =
    EdgeInsets.symmetric(vertical: 20.0, horizontal: 20.0);

const kUserNameTextStyle = TextStyle(
    fontSize: 20.0,
    color: Colors.black,
);

const kSpinKit = SpinKitHourGlass(
    color: Colors.amber,
    size: 70.0,
);

const kFloatingActionIcon = Icon(Icons.add, size: 50,);

const kHeroDuration = Duration(milliseconds: 1300);

const kAppBarText = Text('Profiles');
